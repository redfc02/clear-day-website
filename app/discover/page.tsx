import type { Metadata } from "next";
import { DiscoverPage } from "../clear-day-site";
export const metadata: Metadata = { title: "Discover Clear Day" };
export default function Page(){ return <DiscoverPage/>; }
