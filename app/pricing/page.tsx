import type { Metadata } from "next";
import { PricingPage } from "../clear-day-site";
export const metadata: Metadata = { title: "Pricing — Clear Day" };
export default function Page(){ return <PricingPage/>; }
