import type { Metadata } from "next";
import { FounderStoryPage } from "../clear-day-site";
export const metadata: Metadata = { title: "Founder Story — Clear Day" };
export default function Page(){ return <FounderStoryPage/>; }
